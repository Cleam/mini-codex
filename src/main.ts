/**
 * Mini Codex 入口。
 *
 * 当前阶段（v0.1 Tool Calling）这里只是占位。
 * 后续这个文件会承载整条最小调用链：
 *
 *   user input -> callModel -> tool_use -> validate -> execute -> tool_result -> callModel -> final answer
 *
 * 编排逻辑刻意先集中在 main.ts，跑通之后再按调用链拆分文件，
 * 而不是先按目录结构分配代码。
 */
console.log('mini-codex v0.1 — 工具链就绪，尚未实现 Tool Calling')
